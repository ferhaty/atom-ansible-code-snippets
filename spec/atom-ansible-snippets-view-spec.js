'use babel';

import AtomAnsibleSnippetsView from '../lib/atom-ansible-snippets-view';

describe('AtomAnsibleSnippetsView', () => {
  it('has one valid test', () => {
    expect('life').toBe('easy');
  });
});
